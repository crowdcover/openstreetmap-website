<% set_title(t('group.edit.title', :title => @group.title)) %>

<%= render :partial =>  'heading' %>

<% content_for :heading do %>
  <h2><%= t 'group.edit_group' %>: <%= @group.title %></h2>
<% end %>

<%= render :partial => 'form' %>
